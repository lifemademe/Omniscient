/** Values settled through the editor-only F8 visual tuning panel. */
export const TUNED_BLOOM = {
  strength: 0.045,
  threshold: 0.415,
  radius: 0.509,
} as const;

export const TUNED_OCCLUSION = {
  ssaoStrength: 1.98,
  ssaoRadius: 0.153,
} as const;
